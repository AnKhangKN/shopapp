import 'package:flutter/material.dart';

class Structure26 extends StatelessWidget {
  const Structure26({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ListView(
        children: [
          Image.asset(
            'assets/images/structure26.png',
            width: double.infinity,
            height: 500,
            fit: BoxFit.cover,
          ),
        ],
      ),
    );
  }
}
